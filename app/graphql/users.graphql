query Me {
  me {
    id
    displayName
    photo
    username
  }
}

query User($username: String!) {
  user(username: $username) {
    id
    displayName
    photo
    username
    bio
    posts {
      id
      message
      createdAt
    }
  }
}
